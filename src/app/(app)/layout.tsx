/**
 * Серая страница, белая колонка: так лента отделена от поля и читается
 * как место для чтения, а не как фон по умолчанию. Внутри колонки карточек
 * нет — их роль играет она сама, а материалы разделяются линиями.
 *
 * Шапку рисует каждый раздел: в ленте это дата и шестерёнка, в настройках —
 * заголовок и возврат. Одна на оба означала бы шестерёнку внутри настроек,
 * которая никуда не ведёт.
 */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="mx-auto max-w-3xl px-4 py-4 sm:py-6">
      <div className="rounded-xl border bg-card px-4 py-4 sm:px-6 sm:py-5">{children}</div>
    </div>
  );
}
