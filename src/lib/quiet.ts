/**
 * Что показывается только по наведению.
 *
 * Прозрачностью, а не `hidden`: погашенный кусок остаётся в потоке, держит
 * своё место, и строка не переезжает под курсором. По фокусу внутри группы —
 * тоже: с клавиатуры до спрятанного иначе не добраться. На тапе наведения
 * нет — там видно всё сразу, иначе спрятанное было бы спрятано навсегда.
 *
 * Одна строка на карточку ленты и на блок редактора обзора: две копии
 * договора «как проявляется тихое» разошлись бы на первой же правке.
 * Родитель обязан нести класс `group`.
 */
export const QUIET =
  "opacity-0 transition-opacity group-hover:opacity-100 group-focus-within:opacity-100 [@media(hover:none)]:opacity-100";
